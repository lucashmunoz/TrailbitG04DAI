import React from "react";
import {SafeAreaView, Text} from "react-native";

const SearchMovie = (): React.JSX.Element => {
  return (
    <SafeAreaView>
      <Text>Trailbit - SearchMovie</Text>
    </SafeAreaView>
  );
};

export default SearchMovie;
