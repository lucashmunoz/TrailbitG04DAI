import React from "react";
import { SafeAreaView, Text } from "react-native";

const UserProfile = (): React.JSX.Element => {
  return (
    <SafeAreaView>
      <Text>Trailbit - User Profile</Text>
    </SafeAreaView>
  );
};

export default UserProfile;
